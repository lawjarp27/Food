<div class="logo-container">
    <a href="/" class="logo-link">
        <span class="logo-text">Left2Feed</span>
        <span class="logo-icon">🍽️</span>
    </a>
</div>

<style>
    .logo-container {
        display: flex;
        align-items: center;
    }

    .logo-link {
        display: flex;
        align-items: center;
        text-decoration: none;
        color: white;
        font-weight: 700;
        font-size: 1.8rem;
        transition: var(--transition);
    }

    .logo-text {
        margin-right: 0.5rem;
    }

    .logo-icon {
        font-size: 1.5rem;
        animation: bounce 2s infinite;
    }

    @keyframes bounce {
        0%, 100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-5px);
        }
    }

    .logo-link:hover {
        transform: scale(1.05);
    }

    @media (max-width: 768px) {
        .logo-link {
            font-size: 1.5rem;
        }
    }
</style> 