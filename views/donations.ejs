<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Donations</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #4CAF50;
            --secondary-color: #2196F3;
            --accent-color: #FFC107;
            --text-color: #2c3e50;
            --light-gray: #f5f6fa;
            --border-radius: 12px;
            --box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--light-gray);
            color: var(--text-color);
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .header {
            background: linear-gradient(135deg, var(--primary-color), #45a049);
            padding: 1.5rem;
            box-shadow: var(--box-shadow);
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 3rem;
            color: white;
            border-radius: 0 0 var(--border-radius) var(--border-radius);
        }

        .welcome {
            font-size: 1.8rem;
            font-weight: 600;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
        }

        .back-btn {
            background-color: rgba(255, 255, 255, 0.2);
            color: white;
            border: none;
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            cursor: pointer;
            text-decoration: none;
            font-weight: 500;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .back-btn:hover {
            background-color: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
        }

        .donations-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .donation-card {
            background-color: white;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            padding: 2rem;
            transition: var(--transition);
            position: relative;
            overflow: hidden;
        }

        .donation-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
        }

        .donation-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }

        .donation-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid var(--light-gray);
        }

        .donation-title {
            font-size: 1.4rem;
            color: var(--text-color);
            margin: 0;
            font-weight: 600;
        }

        .donation-age {
            background-color: var(--light-gray);
            padding: 0.5rem 1rem;
            border-radius: 50px;
            font-size: 0.9rem;
            color: var(--text-color);
            font-weight: 500;
        }

        .donation-details {
            margin-bottom: 1.5rem;
        }

        .donation-detail {
            margin-bottom: 1rem;
            display: flex;
            align-items: flex-start;
        }

        .detail-label {
            font-weight: 600;
            color: var(--text-color);
            min-width: 120px;
            opacity: 0.8;
        }

        .donation-address {
            background-color: var(--light-gray);
            padding: 1.5rem;
            border-radius: var(--border-radius);
            margin-top: 1.5rem;
        }

        .nearby-ngos {
            margin-top: 1.5rem;
            border-top: 2px solid var(--light-gray);
            padding-top: 1.5rem;
        }

        .nearby-ngos-title {
            font-size: 1.1rem;
            color: var(--text-color);
            margin-bottom: 1rem;
            font-weight: 600;
        }

        .ngo-list {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .ngo-item {
            background-color: var(--light-gray);
            padding: 1rem;
            border-radius: var(--border-radius);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .ngo-info {
            flex: 1;
        }

        .ngo-name {
            font-weight: 500;
            color: var(--text-color);
        }

        .ngo-distance {
            font-size: 0.9rem;
            color: #666;
        }

        .ngo-email {
            font-size: 0.9rem;
            color: #666;
        }

        .email-link {
            color: #666;
            text-decoration: none;
        }

        .contact-ngo {
            background-color: var(--secondary-color);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 50px;
            text-decoration: none;
            font-size: 0.9rem;
            transition: var(--transition);
        }

        .contact-ngo:hover {
            background-color: #1976D2;
            transform: translateY(-2px);
        }

        .no-donations {
            text-align: center;
            padding: 4rem 2rem;
            background-color: white;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            max-width: 600px;
            margin: 0 auto;
        }

        .no-donations h2 {
            font-size: 2rem;
            color: var(--text-color);
            margin-bottom: 1rem;
        }

        .no-donations p {
            color: #666;
            margin-bottom: 2rem;
        }

        .donate-btn {
            display: inline-block;
            background: linear-gradient(135deg, var(--primary-color), #45a049);
            color: white;
            padding: 1rem 2rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 500;
            transition: var(--transition);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .donate-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 8px rgba(0, 0, 0, 0.15);
        }

        .donation-date {
            position: absolute;
            top: 1rem;
            right: 1rem;
            font-size: 0.8rem;
            color: #666;
            opacity: 0.7;
        }

        @media (max-width: 768px) {
            .donations-grid {
                grid-template-columns: 1fr;
            }
            
            .header {
                flex-direction: column;
                gap: 1rem;
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="welcome">My Donations</div>
        <a href="/" class="back-btn">
            ← Back to Home
        </a>
    </div>
    <div class="container">
        <% if (donations && donations.length > 0) { %>
            <div class="donations-grid">
                <% donations.forEach(donation => { %>
                    <div class="donation-card">
                        <div class="donation-date">
                            <%= new Date(donation.donation_date).toLocaleDateString() %>
                        </div>
                        <div class="donation-header">
                            <h3 class="donation-title"><%= donation.item_name %></h3>
                            <span class="donation-age"><%= donation.food_quality %></span>
                        </div>
                        <div class="donation-details">
                            <div class="donation-detail">
                                <span class="detail-label">Quantity:</span>
                                <span><%= donation.quantity %></span>
                            </div>
                            <% if (donation.description) { %>
                                <div class="donation-detail">
                                    <span class="detail-label">Description:</span>
                                    <span><%= donation.description %></span>
                                </div>
                            <% } %>
                        </div>
                        <div class="donation-address">
                            <div class="donation-detail">
                                <span class="detail-label">Address:</span>
                                <span><%= donation.address %></span>
                            </div>
                            <div class="donation-detail">
                                <span class="detail-label">Pincode:</span>
                                <span><%= donation.pincode %></span>
                            </div>
                        </div>
                        <div class="nearby-ngos">
                            <h4 class="nearby-ngos-title">Nearby NGOs</h4>
                            <% if (donation.nearbyNgos && donation.nearbyNgos.length > 0) { %>
                                <div class="ngo-list">
                                    <% donation.nearbyNgos.forEach(ngo => { %>
                                        <div class="ngo-item">
                                            <div class="ngo-info">
                                                <div class="ngo-name"><%= ngo.ngo_name %></div>
                                                <div class="ngo-distance">
                                                    Pincode: <%= ngo.pincode %>
                                                </div>
                                                <div class="ngo-email">
                                                    <a href="mailto:<%= ngo.email %>" class="email-link"><%= ngo.email %></a>
                                                </div>
                                            </div>
                                        </div>
                                    <% }); %>
                                </div>
                            <% } else { %>
                                <p>No NGOs found within 50 pincodes of this location.</p>
                            <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="no-donations">
                <h2>No Donations Yet</h2>
                <p>Start making a difference by donating food to those in need.</p>
                <a href="/donate" class="donate-btn">Make Your First Donation</a>
            </div>
        <% } %>
    </div>
</body>
</html> 